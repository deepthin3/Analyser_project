             PGM        PARM(&LIBRARIES)

             DCL        VAR(&LIBRARIES) TYPE(*CHAR) LEN(200)
             DCL        VAR(&IFSPATH) TYPE(*CHAR) LEN(100) +
                          VALUE('/home/VAVAN/srcanalysis')
             DCL        VAR(&QSHCMD) TYPE(*CHAR) LEN(300)
             DCL        VAR(&MSGTXT) TYPE(*CHAR) LEN(100)

             CALL       PGM(VAVAN1/SRCEXTRACT) PARM(&LIBRARIES  +
                           &IFSPATH)
             MONMSG     MSGID(CPF0000) EXEC(DO)
             /* Receive the escape message sent from RPG   */
             RCVMSG     PGMQ(*SAME) MSGTYPE(*EXCP) MSG(&MSGTXT)
             SNDPGMMSG  MSG(&MSGTXT)                              +
                          MSGTYPE(*COMP)
             GOTO       CMDLBL(END)
             ENDDO
             SNDPGMMSG  MSG('Source extraction complete'    ) +
                          MSGTYPE(*COMP)
             RETURN
  END:

            ENDPGM
